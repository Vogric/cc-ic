import styles from './Footer.module.css'

const Footer: React.FC = () => {
  return (
    <div className={styles.wrapper}>
        <h1 className={styles.title}>FactoryFour APIs</h1>
    </div>
  )
}

export default Footer